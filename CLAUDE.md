# Claude向けルール

作業開始時に `AGENTS.md`、`docs/ai/共通ルール.md`、`docs/requirements.md`、
`docs/implementation-plan.md`、`docs/test-spec.md` を読むこと。

実装は `apps/source/3dprintingdb/` のみを対象とし、判断が必要な仕様変更は
推測で進めず、ユーザーに確認する。変更後は差分をレビューし、利用可能な
検証コマンドを実行して結果を報告する。
