# 3DprintingDB テスト仕様

## 自動検証

- `npm run typecheck` — TypeScriptの型エラーがないこと。
- `npm run build:web` — GitHub Pages向けのExpo静的サイトが`dist/`に生成できること。
- GitHub Actions「GitHub Pagesへ公開」 — `main`へのpush後に公開が成功すること。

## UI確認

1. 一覧に登録データが表示され、左端チェックボックスを複数選択できる。
2. 行タップで詳細へ移動し、製品画像・推奨値・乾燥条件が表示される。
3. 設定で表示項目と文字サイズを変えると一覧へ反映される。
4. 比較ボタンで選択済みデータの比較画面へ移動できる。
5. 比較項目が12項目表示され、未選択時は空状態を表示する。
6. 作品画像URLを保存し、詳細画面で別画像として表示できる。
7. 画面をスクロールしても一覧ヘッダーが残る。
8. ブラウザーを再起動しても保存したデータが残る。
9. GitHub Pagesの公開URLで一覧・詳細・設定・比較を利用できる。

## 未実施

実サイトからの同期、独自ドメインでの公開確認は、データ利用規約の確認と
ドメイン設定後に実施する。
