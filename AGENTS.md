# 3DprintingDB 開発ルール

このファイルと `docs/ai/` のルールを最初に読むこと。アプリ固有の仕様は
`docs/requirements.md`、作業順は `docs/implementation-plan.md` を正とする。

- 応答・ドキュメント・コミットメッセージは日本語にする。
- カレンダー転記アプリの仕様・認証情報・実装を持ち込まない。
- 外部サイトのデータは出典URLと取得日時を記録し、利用規約に従う。
- `.env`、認証情報、秘密鍵、実機の個人画像をコミットしない。
- 変更後は型チェック、テスト、秘密情報スキャンを実行する。
- 推奨値とユーザー入力値を同じフィールドに上書き保存せず、ラベルで区別する。
- UI変更時も一覧の固定ヘッダー、チェック選択、比較導線を壊さない。
